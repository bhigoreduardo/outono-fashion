import { OfertaprodutoPipe } from './ofertaproduto.pipe';

describe('OfertaprodutoPipe', () => {
  it('create an instance', () => {
    const pipe = new OfertaprodutoPipe();
    expect(pipe).toBeTruthy();
  });
});
