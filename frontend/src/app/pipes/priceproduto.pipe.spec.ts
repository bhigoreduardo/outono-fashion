import { PriceprodutoPipe } from './priceproduto.pipe';

describe('PriceprodutoPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceprodutoPipe();
    expect(pipe).toBeTruthy();
  });
});
