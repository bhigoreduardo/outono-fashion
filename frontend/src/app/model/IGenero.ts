export interface IGenero {
    id: number;
    descricao: string;
}

export interface IGeneroInput {
    id: number;
}

export interface IGeneroModel {
    id: number;
    descricao: string;
}

export interface IGeneroResumoModel {
    descricao: string;
}